<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <title>Admin Dashboard - URL Shortener</title>
  <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="font-sans min-h-screen bg-gradient-to-r from-neutral-700 to-black text-white px-8 py-10">

  <h1 class="text-4xl font-bold text-center mb-2">Admin Dashboard</h1>
  <p class="text-center text-lg mb-6">Welcome, <strong><%= name %></strong> (Admin)</p>

  <% if (urls && urls.length > 0) { %>
    <div class="overflow-x-auto">
      <table class="w-full border-collapse bg-white text-black rounded-lg shadow">
        <thead class="bg-gray-200">
          <tr>
            <th class="py-3 px-4 border border-gray-300">S. No.</th>
            <th class="py-3 px-4 border border-gray-300">Short URL</th>
            <th class="py-3 px-4 border border-gray-300">Original URL</th>
            <th class="py-3 px-4 border border-gray-300">Clicks</th>
            <th class="py-3 px-4 border border-gray-300">Created By</th>
          </tr>
        </thead>
        <tbody>
          <% urls.forEach((url, index) => { %>
            <tr class="even:bg-gray-100 hover:bg-blue-100 transition-colors">
              <td class="py-2 px-4 border border-gray-300"><%= index + 1 %></td>
              <td class="py-2 px-4 border border-gray-300">
                <a href="/url/<%= url.shortId %>" target="_blank" class="text-blue-600 hover:underline">
                  <%= url.shortId %>
                </a>
              </td>
              <td class="py-2 px-4 border border-gray-300"><%= url.redirectURl %></td>
              <td class="py-2 px-4 border border-gray-300"><%= url.visitHistory.length %></td>
              <td class="py-2 px-4 border border-gray-300">
                <% if (url.createdBy) { %>
                  <strong><%= url.createdBy.name %></strong><br />
                  <%= url.createdBy.email %>
                <% } else { %>
                  <em>Unknown</em>
                <% } %>
              </td>
            </tr>
          <% }) %>
        </tbody>
      </table>
    </div>
  <% } else { %>
    <p class="text-center mt-6 text-lg">No URLs found.</p>
  <% } %>

</body>
</html>
